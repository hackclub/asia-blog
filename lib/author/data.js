const map = new Map()

const Set = (key, value) => {
  map.set(key, value)
}

map.set('Athul Blesson', 'athul.jpg')
map.set('Harsh Bajpai', 'harsh.png')
map.set('Annlee Fores', 'annlee.jpg')

export default map
